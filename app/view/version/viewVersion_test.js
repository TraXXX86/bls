'use strict';

describe('myApp.viewVersion module', function () {

    beforeEach(module('myApp.viewVersion'));

    describe('viewVersion controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var viewVersionCtrl = $controller('ViewVersionCtrl');
            expect(viewVersionCtrl).toBeDefined();
        }));

    });
});